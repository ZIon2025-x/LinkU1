# Xcode 签名配置指南

## 🔐 解决 "requires a development team" 错误

### 方法1：在 Xcode 中配置（推荐）

1. **打开项目**
   - 在 Xcode 中打开 `link2ur.xcodeproj`

2. **选择项目**
   - 在左侧导航栏，点击最顶部的项目名称 "link2ur"
   - 确保选中项目（蓝色图标），不是 target

3. **打开 Signing & Capabilities**
   - 在中间面板，选择 "link2ur" target
   - 点击 "Signing & Capabilities" 标签

4. **配置签名**
   - 勾选 "Automatically manage signing"
   - 在 "Team" 下拉菜单中选择你的 Apple ID
   - 如果没有，点击 "Add Account..." 添加

5. **添加 Apple ID**
   - 如果点击 "Add Account..."
   - 输入你的 Apple ID 和密码
   - 等待 Xcode 验证账号

6. **Bundle Identifier**
   - 如果提示 Bundle Identifier 冲突，修改为唯一值
   - 例如：`com.link2ur.ios` 或 `com.yourname.link2ur`

### 方法2：使用免费 Apple ID（无需付费）

**免费账号限制**：
- ✅ 可以在自己的设备上测试
- ✅ 证书有效期 7 天（需要重新签名）
- ❌ 无法发布到 App Store
- ❌ 无法使用某些高级功能

**步骤**：
1. 使用你的 Apple ID（iCloud 账号）
2. 在 Xcode 中添加账号
3. 选择该账号作为 Team
4. Xcode 会自动创建免费证书

### 方法3：付费开发者账号（$99/年）

**付费账号优势**：
- ✅ 证书长期有效
- ✅ 可以发布到 App Store
- ✅ 支持 TestFlight
- ✅ 无设备数量限制

**步骤**：
1. 访问 [Apple Developer](https://developer.apple.com/)
2. 注册开发者账号（$99/年）
3. 在 Xcode 中使用该账号

## 📝 详细步骤（图文说明）

### 步骤 1：选择项目
```
Xcode 左侧导航栏
└── link2ur (蓝色项目图标) ← 点击这里
    └── link2ur (target)
```

### 步骤 2：打开 Signing 设置
```
中间面板顶部标签
General | Signing & Capabilities | Build Settings | ...
                    ↑
              点击这里
```

### 步骤 3：配置签名
```
Signing & Capabilities 面板
┌─────────────────────────────────┐
│ ☑ Automatically manage signing  │ ← 勾选
│                                  │
│ Team: [选择你的 Apple ID]        │ ← 下拉选择
│                                  │
│ Bundle Identifier:                │
│ com.link2ur                      │ ← 如果冲突，修改为唯一值
└─────────────────────────────────┘
```

### 步骤 4：添加账号（如果需要）
```
点击 "Add Account..." 后
┌─────────────────────────────────┐
│ Apple ID: your@email.com        │
│ Password: ********               │
│                                  │
│ [Sign In]                        │
└─────────────────────────────────┘
```

## ⚠️ 常见问题

### 问题1：Bundle Identifier 冲突

**错误信息**：
```
An App ID with Identifier 'com.link2ur' is not available
```

**解决方法**：
- 修改 Bundle Identifier 为唯一值
- 例如：`com.link2ur.ios` 或 `com.yourname.link2ur`

### 问题2：证书创建失败

**错误信息**：
```
Failed to create provisioning profile
```

**解决方法**：
1. 检查网络连接
2. 重新登录 Apple ID
3. 清理构建：Product → Clean Build Folder
4. 删除旧的证书：Xcode → Preferences → Accounts → 选择账号 → Download Manual Profiles

### 问题3：设备未信任

**错误信息**：
```
Device is not registered in your developer account
```

**解决方法**：
1. 在设备上：设置 → 通用 → VPN与设备管理
2. 信任开发者证书
3. 重新连接设备

## 🔧 快速修复命令

如果遇到问题，可以尝试：

1. **清理构建**
   ```bash
   # 在 Xcode 中
   Product → Clean Build Folder (⇧⌘K)
   ```

2. **删除 Derived Data**
   ```bash
   # 在终端中
   rm -rf ~/Library/Developer/Xcode/DerivedData
   ```

3. **重新启动 Xcode**
   - 完全退出 Xcode
   - 重新打开项目

## ✅ 验证配置

配置成功后，你应该看到：
- ✅ "Automatically manage signing" 已勾选
- ✅ Team 显示你的 Apple ID 或团队名称
- ✅ Provisioning Profile 显示 "Xcode Managed Profile"
- ✅ 没有红色错误提示

## 📱 测试

配置完成后：
1. 连接你的 iPhone
2. 在 Xcode 顶部选择设备
3. 点击运行按钮（⌘R）
4. 首次运行需要在设备上信任开发者

## 💡 提示

- **免费账号**：适合个人开发和学习
- **付费账号**：适合正式开发和发布
- **证书过期**：免费账号 7 天后需要重新签名
- **多设备**：免费账号可以注册多个设备（有限制）

