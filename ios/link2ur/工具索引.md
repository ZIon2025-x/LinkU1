# 企业级工具索引

本文档列出了所有可用的企业级工具及其用途。

## 📚 核心架构

### DependencyContainer
**位置**: `Core/DependencyContainer.swift`  
**用途**: 依赖注入容器，管理服务依赖  
**示例**:
```swift
DependencyContainer.shared.register(APIService.self) { APIService() }
let service = DependencyContainer.shared.resolve(APIService.self)
```

### ErrorHandler
**位置**: `Core/ErrorHandler.swift`  
**用途**: 统一错误处理和报告  
**示例**:
```swift
ErrorHandler.shared.handle(error, context: "加载用户")
```

### NetworkManager
**位置**: `Core/NetworkManager.swift`  
**用途**: 网络请求管理，支持缓存、重试、去重  
**示例**:
```swift
NetworkManager.shared.execute(User.self, endpoint: "/api/users/me")
```

### PerformanceMonitor
**位置**: `Core/PerformanceMonitor.swift`  
**用途**: 性能监控和报告  
**示例**:
```swift
PerformanceMonitor.shared.startMonitoring()
```

### SecurityManager
**位置**: `Core/SecurityManager.swift`  
**用途**: 数据加密、安全存储  
**示例**:
```swift
let encrypted = SecurityManager.shared.encrypt(data)
```

### Configuration
**位置**: `Core/Configuration.swift`  
**用途**: 应用配置管理  
**示例**:
```swift
let config = Configuration.shared
let apiURL = config.apiBaseURL
```

## 🔧 属性包装器

### WeakRef
**位置**: `Core/Utils/WeakRef.swift`  
**用途**: 弱引用包装器，避免循环引用  
**示例**:
```swift
@WeakRef var delegate: SomeDelegate?
```

### LazyInitializer
**位置**: `Core/Utils/LazyInitializer.swift`  
**用途**: 延迟初始化  
**示例**:
```swift
@LazyInitializer var service = APIService()
```

### Atomic
**位置**: `Core/Utils/Atomic.swift`  
**用途**: 线程安全的原子值  
**示例**:
```swift
@Atomic var count: Int = 0
$count.update { $0 += 1 }
```

### ExpiringValue
**位置**: `Core/Utils/ExpiringValue.swift`  
**用途**: 带过期时间的值  
**示例**:
```swift
@ExpiringValue(ttl: 3600) var token: String?
```

### PropertyObserver
**位置**: `Core/Utils/PropertyObserver.swift`  
**用途**: 属性变化观察  
**示例**:
```swift
@PropertyObserver var count: Int = 0
$count.sink { print("Changed: \($0)") }
```

### Observable
**位置**: `Core/Utils/PropertyWrapper+Extensions.swift`  
**用途**: 可观察属性  
**示例**:
```swift
@Observable var value: String = ""
$value.sink { print($0) }
```

### UserDefault
**位置**: `Core/Utils/PropertyWrapper+Extensions.swift`  
**用途**: UserDefaults 属性包装器  
**示例**:
```swift
@UserDefault(key: "username", defaultValue: "") var username: String
```

### ThreadSafe
**位置**: `Core/Utils/PropertyWrapper+Extensions.swift`  
**用途**: 线程安全属性  
**示例**:
```swift
@ThreadSafe var data: [String] = []
```

## 🗄️ 数据管理

### StorageManager
**位置**: `Core/Utils/StorageManager.swift`  
**用途**: 统一存储管理  
**示例**:
```swift
StorageManager.shared.setCodable(user, forKey: "user")
let user = StorageManager.shared.getCodable(User.self, forKey: "user")
```

### CacheManager
**位置**: `Core/Utils/CacheManager.swift`  
**用途**: 缓存管理（内存 + 磁盘）  
**示例**:
```swift
try CacheManager.shared.set(user, forKey: "user_123", expiration: 3600)
let user = CacheManager.shared.get(forKey: "user_123", as: User.self)
```

### JSONHelper
**位置**: `Core/Utils/JSONHelper.swift`  
**用途**: JSON 序列化/反序列化  
**示例**:
```swift
let json = JSONHelper.encode(user)
let user = JSONHelper.decode(User.self, from: json)
```

### CompressionHelper
**位置**: `Core/Utils/CompressionHelper.swift`  
**用途**: 数据压缩/解压  
**示例**:
```swift
let compressed = CompressionHelper.compress(data)
let decompressed = CompressionHelper.decompress(compressed)
```

## 🌐 网络工具

### RequestBuilder
**位置**: `Core/Utils/RequestBuilder.swift`  
**用途**: 构建网络请求  
**示例**:
```swift
let request = try RequestBuilder(baseURL: "https://api.example.com", endpoint: "/users")
    .method("GET")
    .header("Authorization", value: "Bearer token")
    .build()
```

### ResponseParser
**位置**: `Core/Utils/ResponseParser.swift`  
**用途**: 解析网络响应  
**示例**:
```swift
let user = try ResponseParser.parseJSON(User.self, from: data)
```

### NetworkInterceptor
**位置**: `Core/Utils/NetworkInterceptor.swift`  
**用途**: 网络请求/响应拦截  
**示例**:
```swift
NetworkInterceptor.shared.addRequestInterceptor { request in
    // 修改请求
    return request
}
```

### RetryManager
**位置**: `Core/Utils/RetryManager.swift`  
**用途**: 重试管理器  
**示例**:
```swift
let result = try await RetryManager.shared.execute(
    { try await loadData() },
    maxAttempts: 3,
    delay: 1.0
)
```

## 📋 任务管理

### TaskQueue
**位置**: `Core/Utils/TaskQueue.swift`  
**用途**: 任务队列（优先级支持）  
**示例**:
```swift
TaskQueue.shared.enqueue(priority: .high) {
    try await uploadImage(image)
}
```

### AsyncOperation
**位置**: `Core/Utils/AsyncOperation.swift`  
**用途**: 异步操作基类  
**示例**:
```swift
class MyOperation: AsyncOperation {
    override func execute() {
        // 执行操作
        finish()
    }
}
```

### Debouncer
**位置**: `Core/Utils/Debouncer.swift`  
**用途**: 防抖工具  
**示例**:
```swift
let debouncer = Debouncer(delay: 0.5)
debouncer.call { search() }
```

### Throttler
**位置**: `Core/Utils/Throttler.swift`  
**用途**: 节流工具  
**示例**:
```swift
let throttler = Throttler(interval: 1.0)
throttler.execute { updateUI() }
```

### Once
**位置**: `Core/Utils/Once.swift`  
**用途**: 一次性执行器  
**示例**:
```swift
let once = Once()
once.execute { initialize() }
```

### Semaphore
**位置**: `Core/Utils/Semaphore.swift`  
**用途**: 信号量控制  
**示例**:
```swift
let semaphore = Semaphore(value: 1)
semaphore.execute { criticalSection() }
```

## 📡 事件系统

### EventBus
**位置**: `Core/Utils/EventBus.swift`  
**用途**: 事件总线（发布/订阅）  
**示例**:
```swift
// 发布
EventBus.shared.publish(UserLoginEvent(userId: "123"))

// 订阅
EventBus.shared.subscribe(UserLoginEvent.self)
    .sink { event in print(event.userId) }
```

### KeyValueObserver
**位置**: `Core/Utils/KeyValueObserver.swift`  
**用途**: KVO 观察器  
**示例**:
```swift
let observer = KeyValueObserver()
observer.observe(object, keyPath: \.value) { newValue in
    print(newValue)
}
```

## 🎨 UI 工具

### LoadingState
**位置**: `Core/Components/LoadingState.swift`  
**用途**: 加载状态管理  
**示例**:
```swift
@Published var loadingState: LoadingState<User> = .idle
.loadingState(loadingState)
```

### RefreshableScrollView
**位置**: `Core/Components/RefreshableScrollView.swift`  
**用途**: 可刷新滚动视图  
**示例**:
```swift
RefreshableScrollView(onRefresh: { await refresh() }) {
    // 内容
}
```

### PaginatedList
**位置**: `Core/Components/PaginatedList.swift`  
**用途**: 分页列表  
**示例**:
```swift
PaginatedList(items: items, loadMore: loadMore, hasMore: hasMore) { item in
    ItemRow(item: item)
}
```

### AnimationHelper
**位置**: `Core/Utils/AnimationHelper.swift`  
**用途**: 动画辅助  
**示例**:
```swift
withAnimation(AnimationHelper.springAnimation) {
    // 动画
}
```

### HapticFeedback
**位置**: `Core/Utils/HapticFeedback.swift`  
**用途**: 触觉反馈  
**示例**:
```swift
HapticFeedback.shared.impact(.medium)
```

### KeyboardDismiss
**位置**: `Core/Utils/KeyboardDismiss.swift`  
**用途**: 关闭键盘  
**示例**:
```swift
.hideKeyboard()
```

### ViewInspector
**位置**: `Core/Utils/ViewInspector.swift`  
**用途**: 视图调试工具（DEBUG）  
**示例**:
```swift
.inspect(name: "UserView", color: .red)
```

## 📊 格式化工具

### TimeFormatter
**位置**: `Core/Utils/TimeFormatter.swift`  
**用途**: 时间格式化  
**示例**:
```swift
TimeFormatter.formatRelativeTime(date)
TimeFormatter.formatFullDateTime(date)
```

### NumberFormatterHelper
**位置**: `Core/Utils/NumberFormatterHelper.swift`  
**用途**: 数字格式化  
**示例**:
```swift
NumberFormatterHelper.formatCurrency(100.0)
NumberFormatterHelper.formatPercentage(0.5)
```

## 🔍 验证工具

### ValidationHelper
**位置**: `Core/Utils/ValidationHelper.swift`  
**用途**: 数据验证  
**示例**:
```swift
ValidationHelper.isValidEmail(email)
ValidationHelper.isValidPhoneNumber(phone)
```

## 📱 系统集成

### PermissionManager
**位置**: `Core/Utils/PermissionManager.swift`  
**用途**: 权限管理  
**示例**:
```swift
PermissionManager.shared.requestPermission(.camera) { granted in
    // 处理结果
}
```

### DeepLinkHandler
**位置**: `Core/Utils/DeepLinkHandler.swift`  
**用途**: 深度链接处理  
**示例**:
```swift
DeepLinkHandler.shared.handle(url: url)
```

### AppReview
**位置**: `Core/Utils/AppReview.swift`  
**用途**: 应用评价  
**示例**:
```swift
AppReview.requestReview()
```

### Clipboard
**位置**: `Core/Utils/Clipboard.swift`  
**用途**: 剪贴板操作  
**示例**:
```swift
Clipboard.copy(text)
let text = Clipboard.paste()
```

### ShareSheet
**位置**: `Core/Utils/ShareSheet.swift`  
**用途**: 分享功能  
**示例**:
```swift
ShareSheet.share(items: [text], in: viewController)
```

## 📈 监控和分析

### Analytics
**位置**: `Core/Utils/Analytics.swift`  
**用途**: 事件分析  
**示例**:
```swift
Analytics.shared.trackEvent("user_login", parameters: ["userId": "123"])
```

### CrashReporter
**位置**: `Core/Utils/CrashReporter.swift`  
**用途**: 崩溃报告  
**示例**:
```swift
CrashReporter.shared.recordCrash(reason: "内存不足")
```

### MemoryMonitor
**位置**: `Core/Utils/MemoryMonitor.swift`  
**用途**: 内存监控  
**示例**:
```swift
MemoryMonitor.shared.currentMemoryUsage
```

### AppMetrics
**位置**: `Core/Utils/AppMetrics.swift`  
**用途**: 指标收集  
**示例**:
```swift
AppMetrics.shared.record(name: "api_call", value: 1)
```

## 🎛️ 配置管理

### FeatureToggle
**位置**: `Core/Utils/FeatureToggle.swift`  
**用途**: 特性开关  
**示例**:
```swift
if FeatureToggle.shared.isEnabled("analytics") {
    // 启用分析
}
```

### AppStateManager
**位置**: `Core/Utils/AppStateManager.swift`  
**用途**: 应用状态管理  
**示例**:
```swift
AppStateManager.shared.updateUser(userId: "123")
```

## 🛠️ 实用工具

### DeviceInfo
**位置**: `Core/Utils/DeviceInfo.swift`  
**用途**: 设备信息  
**示例**:
```swift
DeviceInfo.model
DeviceInfo.osVersion
```

### AppVersion
**位置**: `Core/Utils/AppVersion.swift`  
**用途**: 应用版本信息  
**示例**:
```swift
AppVersion.current
AppVersion.build
```

### AppLifecycle
**位置**: `Core/Utils/AppLifecycle.swift`  
**用途**: 应用生命周期  
**示例**:
```swift
AppLifecycle.shared.didEnterBackground
    .sink { /* 处理 */ }
```

### Reachability
**位置**: `Core/Utils/Reachability.swift`  
**用途**: 网络可达性  
**示例**:
```swift
Reachability.shared.isConnected
```

### ImageCache
**位置**: `Core/Utils/ImageCache.swift`  
**用途**: 图片缓存  
**示例**:
```swift
ImageCache.shared.set(image, forKey: "avatar")
let image = ImageCache.shared.get(forKey: "avatar")
```

### ImageProcessor
**位置**: `Core/Utils/ImageProcessor.swift`  
**用途**: 图片处理  
**示例**:
```swift
let resized = ImageProcessor.resize(image, to: CGSize(width: 100, height: 100))
```

### QRCodeGenerator
**位置**: `Core/Utils/QRCodeGenerator.swift`  
**用途**: 二维码生成  
**示例**:
```swift
let qrCode = QRCodeGenerator.generateQRCode(from: "https://example.com")
```

### BackupManager
**位置**: `Core/Utils/BackupManager.swift`  
**用途**: 备份管理  
**示例**:
```swift
try await BackupManager.shared.createBackup()
```

### CodeGenerator
**位置**: `Core/Utils/CodeGenerator.swift`  
**用途**: 代码生成  
**示例**:
```swift
let template = CodeGenerator.generateViewModelTemplate(name: "User", modelName: "User")
```

### LocalizationHelper
**位置**: `Core/Utils/LocalizationHelper.swift`  
**用途**: 本地化辅助  
**示例**:
```swift
LocalizationHelper.localized("welcome")
```

### AppTheme
**位置**: `Core/Utils/AppTheme.swift`  
**用途**: 主题管理  
**示例**:
```swift
AppTheme.shared.setTheme(.dark)
```

### Logger
**位置**: `Utils/Logger.swift`  
**用途**: 日志系统  
**示例**:
```swift
Logger.debug("调试信息", category: .api)
Logger.error("错误信息", category: .api)
Logger.success("成功信息", category: .api)
```

## 📚 扩展方法

### String+Extensions
**位置**: `Core/Extensions/String+Extensions.swift`  
**用途**: String 扩展方法  
**示例**:
```swift
email.isValidEmail
phone.isValidPhoneNumber
```

### Date+Extensions
**位置**: `Core/Extensions/Date+Extensions.swift`  
**用途**: Date 扩展方法  
**示例**:
```swift
date.relativeTime
date.isToday
```

### Array+Extensions
**位置**: `Core/Extensions/Array+Extensions.swift`  
**用途**: Array 扩展方法  
**示例**:
```swift
array.safeSubscript(index: 0)
array.chunked(size: 10)
```

### Dictionary+Extensions
**位置**: `Core/Extensions/Dictionary+Extensions.swift`  
**用途**: Dictionary 扩展方法  
**示例**:
```swift
dict.merging(otherDict)
dict.safeSubscript(key: "key")
```

### URL+Extensions
**位置**: `Core/Extensions/URL+Extensions.swift`  
**用途**: URL 扩展方法  
**示例**:
```swift
url.appendingQueryParameters(["key": "value"])
```

### View+Extensions
**位置**: `Core/Extensions/View+Extensions.swift`  
**用途**: View 扩展方法  
**示例**:
```swift
view.loadingOverlay(isLoading: true)
view.errorAlert(error: error)
```

### Publisher+Extensions
**位置**: `Core/Extensions/Publisher+Extensions.swift`  
**用途**: Publisher 扩展方法  
**示例**:
```swift
publisher.retryOnFailure(maxAttempts: 3)
publisher.handleError { error in }
```

## 🧪 测试工具

### TestHelpers
**位置**: `Core/Testing/TestHelpers.swift`  
**用途**: 测试辅助函数  
**示例**:
```swift
expectFailure { throw error }
```

### MockAPIService
**位置**: `Core/Testing/MockAPIService.swift`  
**用途**: Mock API 服务  
**示例**:
```swift
let mockService = MockAPIService()
mockService.mockResponse(User.self, data: userData)
```

## 📖 使用建议

1. **优先使用核心工具**: DependencyContainer, ErrorHandler, NetworkManager
2. **选择合适的属性包装器**: 根据需求选择 WeakRef, Atomic, ExpiringValue 等
3. **利用扩展方法**: 减少重复代码，提高可读性
4. **使用事件系统**: EventBus 用于组件间通信
5. **监控和分析**: 使用 Analytics, CrashReporter 等工具

## 🔗 相关文档

- [企业级优化完整总结](./企业级优化完整总结.md)
- [迁移指南](./迁移指南.md)
- [代码文档规范](./link2ur/Core/CodeDocumentation.md)

