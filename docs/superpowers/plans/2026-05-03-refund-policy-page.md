# Refund Policy Page Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add an in-app, version-controlled, multilingual Refund Policy page reachable from a consent footer beneath the Pay button on every payment screen and from settings.

**Architecture:** Backend-driven via the existing `/api/legal/{type}?lang=` endpoint. SQL migration 223 seeds two `legal_documents` rows (`refund_policy` × `zh` / `en`). A new `RefundPolicyView` (FutureBuilder + `LegalDocument.fromJson`) renders ordered `LegalSection`s. A new `RefundPolicyFooter` widget is dropped under the Pay button on `PaymentView` and `ApprovalPaymentPage`. A nav row in settings' "法律条款" section provides a non-payment entry.

**Tech Stack:** Flutter 3.33+, BLoC (no new bloc — read-only fetch via `CommonRepository`), GoRouter, ARB l10n. Backend: PostgreSQL (no Python changes — endpoint is type-agnostic).

**Spec:** `docs/superpowers/specs/2026-05-03-refund-policy-page-design.md`

---

## File Structure

**New files:**
- `backend/migrations/223_seed_refund_policy_legal_document.sql` — two `INSERT INTO legal_documents` rows (zh, en)
- `link2ur/lib/features/info/views/refund_policy_view.dart` — `RefundPolicyView` (Scaffold + FutureBuilder of `getLegalDocument` + ListView of section cards)
- `link2ur/lib/features/payment/views/widgets/refund_policy_footer.dart` — reusable consent footer (`StatefulWidget` to dispose `TapGestureRecognizer`)
- `link2ur/test/data/models/legal_document_test.dart` — unit test for `_orderKeys('refund_policy')` ordering
- `link2ur/test/features/payment/widgets/refund_policy_footer_test.dart` — widget test for footer link tap

**Modified files:**
- `link2ur/lib/data/models/legal_document.dart:74-110` — add `case 'refund_policy'` in `_orderKeys()`
- `link2ur/lib/core/router/app_routes.dart:158-166` — add `static const String refundPolicy = '/info/refund-policy';`
- `link2ur/lib/core/router/routes/info_routes.dart:11-58` — register `GoRoute` for refund policy
- `link2ur/lib/features/payment/views/payment_view.dart` (around line 1142, in `_buildPayButton`) — embed `const RefundPolicyFooter()` under the `PrimaryButton`
- `link2ur/lib/features/tasks/views/approval_payment_page.dart:978` — embed `const RefundPolicyFooter()` under the `PrimaryButton`
- `link2ur/lib/features/settings/views/settings_view.dart:304-344` — add a `_SettingsNavRow` for refund policy in the "法律条款" `_SettingsSection`
- `link2ur/lib/l10n/app_zh.arb` + `app_en.arb` + `app_zh_Hant.arb` — add 3 new keys: `refundPolicyTitle`, `refundPolicyFooterPrefix`, `refundPolicyLinkText`

**Generated (do not hand-edit):**
- `link2ur/lib/l10n/app_localizations*.dart` — regenerated by `flutter gen-l10n`

---

## Notes for Implementer

**Environment setup before any Flutter command:**
```powershell
$env:PATH = "F:\flutter\bin;" + $env:PATH; $env:PUB_CACHE = "F:\DevCache\.pub-cache"; $env:GRADLE_USER_HOME = "F:\DevCache\.gradle"
```
Run all `flutter` commands from `link2ur/` subdirectory.

**Migration-before-deploy rule:** Per `MEMORY.md` (`feedback_migration_before_deploy.md`), DB migration 223 MUST be run on the linktest DB **before** pushing the Flutter changes that depend on it. Otherwise Railway autodeploys the new code, the API call returns 404, and the in-app page breaks. Order in this plan reflects that.

**Solo project, push to main:** Per `MEMORY.md` (`feedback_direct_to_main.md`), commit each task's work directly to `main` after the test passes. Do not create feature branches.

**Wallet has no top-up flow:** spec mentioned wallet top-up as a 4th entry, but `wallet_view.dart` only displays balance / transactions / Stripe Connect status — there is no user-initiated payment flow on this screen. **Footer is wired into 2 payment surfaces only** (PaymentView + ApprovalPaymentPage), plus the settings nav row.

---

## Task 1: Author backend migration 223 (zh + en seed)

**Files:**
- Create: `backend/migrations/223_seed_refund_policy_legal_document.sql`

- [ ] **Step 1: Create the migration file with both language payloads**

Create `backend/migrations/223_seed_refund_policy_legal_document.sql`:

```sql
-- 迁移 223：新增退款政策法律文档（refund_policy）
-- 依赖：076（legal_documents 表）
-- 在支付页脚 + 设置页"法律条款"提供独立的"退款政策"入口
-- 幂等：使用 ON CONFLICT (type, lang) DO UPDATE
-- 注意：legal_documents 表的唯一约束应为 (type, lang)；如约束名不同请按实际调整。

-- ========== 中文 (zh) ==========
INSERT INTO legal_documents (type, lang, content_json, version, effective_at)
VALUES ('refund_policy', 'zh', $REFUND_ZH$
{
  "title": "退款政策",
  "lastUpdated": "最后更新：2026-05-03",
  "version": "版本：v1.0",
  "effectiveDate": "生效日期：2026-05-03",
  "intro": "本政策适用于 Link²Ur 平台所有付费场景，包括但不限于：任务委托、套餐购买、活动报名、跳蚤市场租赁。VIP 订阅通过 Apple App Store / Google Play 购买，退款须按对应商店规则办理（详见第 12 节）。",
  "eligibility": "退款申请须满足以下条件之一：任务尚未确认完成（状态为「待确认」之前）；双方协商一致取消；服务方违反平台规则被处理；平台原因导致服务无法继续。一旦任务双方确认完成，原则上不可申请退款。",
  "fullRefund": "以下情形可申请全额退款：（1）服务方未开始履约；（2）双方协商一致取消任务；（3）服务方逾期未响应或单方放弃；（4）服务方违反平台规则被处理（如封号、禁言）；（5）平台原因（系统故障、不可抗力、运营调整）导致服务无法继续。",
  "partialRefund": "以下情形可申请按比例退款：（1）服务方已部分交付，任务部分完成；（2）双方就部分履约达成协议；（3）因发布方原因中止但服务方已投入工作。退款比例（0%–100%）由双方协商或平台审核裁定，通常按已完成工作量、已发生成本、约定服务标准等因素综合判定。",
  "nonRefundable": "以下情形不予退款：（1）任务已确认完成且双方均已评价；（2）服务方按约履约后，因发布方个人原因取消；（3）提供虚假证据或恶意申请退款；（4）申请超出本政策规定的有效期或任务状态窗口。",
  "refundProcess": "退款流程：（1）任务发布方在任务详情页选择「申请退款」；（2）选择退款类型（全额/按比例）、退款原因、退款金额；（3）服务方在 48 小时内回应（同意/异议）；（4）双方达成一致后退款进入处理；（5）双方未达成一致时申请平台介入，客服在 3-5 个工作日内裁定；（6）审核通过后按原支付路径退款。",
  "refundTime": "退款到账时效（以渠道实际为准）：钱包/平台余额——即时；银行卡 / Apple Pay——5-10 个工作日；微信支付——3-7 个工作日；支付宝——3-7 个工作日。退款失败的（如卡片已注销），平台将联系您协商替代方案。",
  "walletAndCoupon": "钱包与优惠券处理：（1）使用钱包余额抵扣的部分，退款时退回钱包；Stripe 渠道支付的部分，退回原卡。（2）全额退款时，使用过的优惠券将返还到您的账户（可在有效期内重复使用）；按比例退款时，优惠券视为已使用，不予返还。（3）积分抵扣的处理同优惠券规则。",
  "disputeResolution": "争议处理：双方协商不成时，任意一方可申请平台介入。客服将在 3-5 个工作日内基于聊天记录、任务进度证据、双方陈述等综合裁定。平台裁定为最终结果，双方应予执行。涉及金额较大或复杂争议的，平台可延长审核期并要求补充证据。",
  "vipSubscription": "VIP 订阅退款：Link²Ur VIP 通过 Apple App Store / Google Play 的应用内购买进行，退款须直接联系 Apple 支持或 Google Play 客服办理。平台无法直接处理通过应用商店购买的 VIP 订阅退款。已取消的订阅在当前付费周期结束前仍可享受 VIP 权益。",
  "specialCases": "特殊场景：（1）不可抗力（自然灾害、政府管制、突发公共事件）导致任务无法继续的，按平台公告处理；（2）平台原因（系统故障、运营调整）导致的损失，平台将主动全额退款并视情况提供补偿；（3）账号违规处置：被处置账号涉及的待结算资金按平台规则与法律要求处理。",
  "contactUs": "如有疑问或需要协助，请通过应用内「我的 → 帮助中心 → 联系客服」联系我们，客服工作时间为周一至周五 09:00-18:00（英国时间）。也可发送邮件至 info@link2ur.com。",
  "importantNotice": "本政策的最终解释权归 Link²Ur 平台所有。本政策可能因业务调整或法律法规变化而更新，更新后将在应用内公示并以最新版本为准。继续使用本平台付费功能视为接受最新版本的退款政策。"
}
$REFUND_ZH$, 'v1.0', '2026-05-03'::date)
ON CONFLICT (type, lang) DO UPDATE
SET content_json = EXCLUDED.content_json,
    version = EXCLUDED.version,
    effective_at = EXCLUDED.effective_at,
    updated_at = NOW();

-- ========== English (en) ==========
INSERT INTO legal_documents (type, lang, content_json, version, effective_at)
VALUES ('refund_policy', 'en', $REFUND_EN$
{
  "title": "Refund Policy",
  "lastUpdated": "Last updated: 2026-05-03",
  "version": "Version: v1.0",
  "effectiveDate": "Effective date: 2026-05-03",
  "intro": "This policy applies to all paid transactions on the Link²Ur platform, including but not limited to: task commissioning, service package purchases, paid activity sign-ups, and flea-market rentals. VIP subscriptions are purchased via Apple App Store / Google Play; refunds must be requested through the relevant store (see Section 12).",
  "eligibility": "A refund request must meet at least one of the following conditions: the task has not yet been confirmed as completed (i.e. is still before the 'pending confirmation' stage); both parties agree to cancel; the service provider has been sanctioned by the platform for breaching the rules; or platform-side reasons make the service impossible to continue. Once both parties confirm the task is completed, refunds are generally not available.",
  "fullRefund": "A full refund may be requested in the following situations: (1) the service provider has not started performance; (2) both parties have agreed to cancel the task; (3) the service provider has not responded within the agreed time or has unilaterally abandoned the task; (4) the service provider has been sanctioned by the platform for rule violations (e.g. account suspension, mute); (5) platform-side reasons (system fault, force majeure, operational changes) make the service impossible to continue.",
  "partialRefund": "A pro-rata refund may be requested in the following situations: (1) the service provider has partially delivered and the task is partially completed; (2) both parties have reached agreement on partial performance; (3) the task was suspended due to the poster's reasons after the service provider had already invested work. The refund proportion (0%–100%) is determined either by agreement between the parties or by platform review, typically based on the work already completed, costs already incurred, and the agreed service standard.",
  "nonRefundable": "Refunds are not available in the following situations: (1) the task has been confirmed as completed and both parties have left reviews; (2) the service provider has performed as agreed and the poster cancels for personal reasons; (3) false evidence is provided or the refund request is made in bad faith; (4) the request falls outside the valid window or task-status window defined by this policy.",
  "refundProcess": "Refund process: (1) the task poster selects 'Request refund' on the task detail page; (2) chooses the refund type (full / pro-rata), reason and amount; (3) the service provider responds within 48 hours (accept / dispute); (4) once both parties agree, the refund proceeds to processing; (5) where the parties cannot agree, either side may request platform intervention, and our support team will issue a decision within 3-5 working days; (6) once approved, the refund is returned via the original payment route.",
  "refundTime": "Refund arrival time (subject to the actual channel): wallet / platform balance — instant; bank card / Apple Pay — 5-10 working days; WeChat Pay — 3-7 working days; Alipay — 3-7 working days. If a refund fails (for example, the card has been cancelled), the platform will contact you to arrange an alternative.",
  "walletAndCoupon": "Wallet and coupon handling: (1) the portion paid by wallet balance is refunded to the wallet; the portion paid via Stripe is refunded to the original card. (2) On a full refund, any coupon used will be returned to your account and may be re-used within its validity; on a pro-rata refund, the coupon is treated as used and will not be returned. (3) Points used for deduction follow the same rules as coupons.",
  "disputeResolution": "Dispute resolution: if the parties cannot agree, either side may request platform intervention. Our support team will reach a decision within 3-5 working days based on chat records, task-progress evidence and both parties' statements. The platform's decision is final and binding. For larger amounts or complex disputes, the platform may extend the review period and request additional evidence.",
  "vipSubscription": "VIP subscription refunds: Link²Ur VIP is purchased via Apple App Store / Google Play in-app purchase. Refunds must be requested directly through Apple Support or Google Play support — the platform cannot process refunds for VIP subscriptions bought through the app stores. A cancelled subscription continues to receive VIP benefits until the end of the current paid period.",
  "specialCases": "Special situations: (1) where force majeure (natural disasters, government measures, public-health emergencies) makes a task impossible to continue, the platform will handle it according to public notices; (2) for losses caused by platform-side reasons (system fault, operational changes), the platform will proactively issue a full refund and may provide additional compensation as appropriate; (3) for accounts subject to violation handling, any unsettled funds will be handled in accordance with platform rules and applicable law.",
  "contactUs": "If you have questions or need help, please contact us via 'Profile → Help Centre → Contact Support' in the app. Support hours are Monday to Friday, 09:00-18:00 (UK time). You can also email info@link2ur.com.",
  "importantNotice": "The final right of interpretation of this policy rests with the Link²Ur platform. This policy may be updated due to business changes or changes in law, and the latest version will be published in the app and prevail. Continuing to use the paid features of the platform constitutes acceptance of the latest version of this Refund Policy."
}
$REFUND_EN$, 'v1.0', '2026-05-03'::date)
ON CONFLICT (type, lang) DO UPDATE
SET content_json = EXCLUDED.content_json,
    version = EXCLUDED.version,
    effective_at = EXCLUDED.effective_at,
    updated_at = NOW();

-- 验证
-- SELECT type, lang, version, effective_at FROM legal_documents WHERE type = 'refund_policy';
-- 期望：2 行 (zh + en)，version='v1.0'，effective_at='2026-05-03'
```

- [ ] **Step 2: Verify the SQL parses by inspection (no run yet)**

The actual run happens in Task 2. For now, just visually confirm:
- Both `INSERT INTO` blocks are present
- Both dollar-quote tags `$REFUND_ZH$` and `$REFUND_EN$` are matched (open + close)
- Both rows have all 14 content keys + 4 metadata keys
- ON CONFLICT clauses match (assumes a unique constraint on `(type, lang)` — the existing migrations 077/078 use the same key combination as a `WHERE` filter, indicating the constraint exists)

- [ ] **Step 3: Commit the migration file**

```bash
git add backend/migrations/223_seed_refund_policy_legal_document.sql
git commit -m "feat(legal): seed refund_policy legal document (zh + en)"
```

---

## Task 2: Run migration 223 on linktest DB and smoke-test the API

**Files:** None modified. This is a deployment step.

- [ ] **Step 1: Get the linktest DB URL**

The DB URL is in Railway dashboard under the `linktest` service environment variables (`DATABASE_URL`). Set it as a local env var:

```powershell
$env:LINKTEST_DB = "<DATABASE_URL from Railway linktest>"
```

If you don't have access, ask the human to run Steps 2-4 and report back the API smoke-test result.

- [ ] **Step 2: Run the migration**

```powershell
psql $env:LINKTEST_DB -f F:\python_work\LinkU\backend\migrations\223_seed_refund_policy_legal_document.sql
```

Expected output: two `INSERT 0 1` lines, exit code 0. If you see `ERROR: duplicate key value`, the row already existed — re-run after confirming the `ON CONFLICT` clause matches the actual unique constraint name on `legal_documents`.

- [ ] **Step 3: Verify the rows are present**

```powershell
psql $env:LINKTEST_DB -c "SELECT type, lang, version, effective_at FROM legal_documents WHERE type='refund_policy' ORDER BY lang;"
```

Expected:
```
     type      | lang | version | effective_at
---------------+------+---------+--------------
 refund_policy | en   | v1.0    | 2026-05-03
 refund_policy | zh   | v1.0    | 2026-05-03
```

- [ ] **Step 4: Smoke-test the public API endpoint**

```bash
curl -s "https://linktest.up.railway.app/api/legal/refund_policy?lang=zh" | jq '.type, .lang, .version, (.content_json | keys | length)'
curl -s "https://linktest.up.railway.app/api/legal/refund_policy?lang=en" | jq '.type, .lang, .version, (.content_json | keys | length)'
```

Expected for each:
```
"refund_policy"
"zh"   # or "en"
"v1.0"
18     # 4 metadata keys + 14 content keys
```

If the curl returns `404` or an error JSON, fix before proceeding to the frontend tasks (do NOT push frontend changes that will break for users).

---

## Task 3: Add `_orderKeys('refund_policy')` case + unit test

**Files:**
- Modify: `link2ur/lib/data/models/legal_document.dart:73-110`
- Create: `link2ur/test/data/models/legal_document_test.dart`

- [ ] **Step 1: Write the failing test**

Create `link2ur/test/data/models/legal_document_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:link2ur/data/models/legal_document.dart';

void main() {
  group('LegalDocument._orderKeys via sections (refund_policy)', () {
    test('renders refund_policy sections in the documented order', () {
      final doc = LegalDocument.fromJson({
        'type': 'refund_policy',
        'lang': 'zh',
        'version': 'v1.0',
        'content_json': {
          'title': '退款政策',
          'lastUpdated': '最后更新：2026-05-03',
          'version': '版本：v1.0',
          'effectiveDate': '生效日期：2026-05-03',
          'intro': 'intro body',
          'eligibility': 'eligibility body',
          'fullRefund': 'full body',
          'partialRefund': 'partial body',
          'nonRefundable': 'non body',
          'refundProcess': 'process body',
          'refundTime': 'time body',
          'walletAndCoupon': 'wallet body',
          'disputeResolution': 'dispute body',
          'vipSubscription': 'vip body',
          'specialCases': 'special body',
          'contactUs': 'contact body',
          'importantNotice': 'notice body',
        },
      });

      final titles = doc.sections.map((s) => s.title).toList();

      // _orderKeys skips 'title'; metadata strings (lastUpdated, version,
      // effectiveDate) become single-paragraph sections with the key as title.
      expect(titles, [
        'lastUpdated',
        'version',
        'effectiveDate',
        'intro',
        'eligibility',
        'fullRefund',
        'partialRefund',
        'nonRefundable',
        'refundProcess',
        'refundTime',
        'walletAndCoupon',
        'disputeResolution',
        'vipSubscription',
        'specialCases',
        'contactUs',
        'importantNotice',
      ]);
    });

    test('omits keys absent from content_json without error', () {
      final doc = LegalDocument.fromJson({
        'type': 'refund_policy',
        'lang': 'en',
        'content_json': {
          'title': 'Refund Policy',
          'intro': 'intro body',
          'fullRefund': 'full body',
        },
      });

      final titles = doc.sections.map((s) => s.title).toList();
      expect(titles, ['intro', 'fullRefund']);
    });
  });
}
```

- [ ] **Step 2: Run the test to verify it fails**

```powershell
cd F:\python_work\LinkU\link2ur
$env:PATH = "F:\flutter\bin;" + $env:PATH; $env:PUB_CACHE = "F:\DevCache\.pub-cache"
flutter test test/data/models/legal_document_test.dart
```

Expected: BOTH tests FAIL. The first test fails because `_orderKeys('refund_policy')` returns `[]`, falling into `_sectionsFallback` which sorts alphabetically (so order won't match). The second test may also fail for the same reason (alphabetical, not insertion order).

- [ ] **Step 3: Add the `case 'refund_policy'` branch**

Edit `link2ur/lib/data/models/legal_document.dart`. In `_orderKeys()` (around line 73-110), insert this case **before** the `case 'privacy':` branch (so the order matches existing alphabetical-ish style: terms, refund_policy, community_guidelines, privacy, cookie):

```dart
      case 'refund_policy':
        return [
          'title', 'lastUpdated', 'version', 'effectiveDate',
          'intro', 'eligibility', 'fullRefund', 'partialRefund',
          'nonRefundable', 'refundProcess', 'refundTime',
          'walletAndCoupon', 'disputeResolution', 'vipSubscription',
          'specialCases', 'contactUs', 'importantNotice',
        ];
```

The full updated `_orderKeys` should look like (showing context):

```dart
  static List<String> _orderKeys(String documentType) {
    switch (documentType) {
      case 'terms':
        return [
          'title', 'lastUpdated', 'version', 'effectiveDate', 'jurisdiction',
          // ... (existing keys, do not modify) ...
          'studentVerificationTerms', 'importantNotice',
        ];
      case 'refund_policy':
        return [
          'title', 'lastUpdated', 'version', 'effectiveDate',
          'intro', 'eligibility', 'fullRefund', 'partialRefund',
          'nonRefundable', 'refundProcess', 'refundTime',
          'walletAndCoupon', 'disputeResolution', 'vipSubscription',
          'specialCases', 'contactUs', 'importantNotice',
        ];
      case 'community_guidelines':
        // ... (existing, do not modify) ...
      case 'privacy':
        // ... (existing, do not modify) ...
      case 'cookie':
        // ... (existing, do not modify) ...
      default:
        return [];
    }
  }
```

- [ ] **Step 4: Run the test to verify it passes**

```powershell
flutter test test/data/models/legal_document_test.dart
```

Expected: BOTH tests PASS.

- [ ] **Step 5: Commit**

```bash
git add link2ur/lib/data/models/legal_document.dart link2ur/test/data/models/legal_document_test.dart
git commit -m "feat(legal): add refund_policy key ordering + unit test"
```

---

## Task 4: Add l10n keys to all three ARB files + regenerate

**Files:**
- Modify: `link2ur/lib/l10n/app_zh.arb`
- Modify: `link2ur/lib/l10n/app_en.arb`
- Modify: `link2ur/lib/l10n/app_zh_Hant.arb`
- Generated: `link2ur/lib/l10n/app_localizations*.dart` (via `flutter gen-l10n`)

- [ ] **Step 1: Add keys to `app_zh.arb`**

Add these three lines somewhere in the JSON object (the file is one large `{ ... }`; insert near similarly-themed `payment*` keys for cohesion). Use this exact JSON snippet:

```json
  "refundPolicyTitle": "退款政策",
  "refundPolicyFooterPrefix": "支付即视为已阅读并同意",
  "refundPolicyLinkText": "《退款政策》",
```

Make sure each line ends with a trailing comma (the file is JSON; the next-following line must already have a comma OR you must add one). Verify the file still parses as valid JSON before continuing.

- [ ] **Step 2: Add the same keys to `app_en.arb`**

```json
  "refundPolicyTitle": "Refund Policy",
  "refundPolicyFooterPrefix": "By tapping Pay, you agree to our ",
  "refundPolicyLinkText": "Refund Policy",
```

(Note the trailing space after "our " — it concatenates with the link text.)

- [ ] **Step 3: Add the same keys to `app_zh_Hant.arb`**

```json
  "refundPolicyTitle": "退款政策",
  "refundPolicyFooterPrefix": "支付即視為已閱讀並同意",
  "refundPolicyLinkText": "《退款政策》",
```

- [ ] **Step 4: Regenerate localization files**

```powershell
cd F:\python_work\LinkU\link2ur
$env:PATH = "F:\flutter\bin;" + $env:PATH; $env:PUB_CACHE = "F:\DevCache\.pub-cache"
flutter gen-l10n
```

Expected output: a few INFO lines, no errors. Newly-touched files:
- `lib/l10n/app_localizations.dart`
- `lib/l10n/app_localizations_zh.dart`
- `lib/l10n/app_localizations_en.dart`

- [ ] **Step 5: Verify the generated getters exist**

```powershell
flutter analyze lib/l10n/
```

Expected: no errors. Then quick sanity grep:

```bash
grep -c "refundPolicyTitle\|refundPolicyFooterPrefix\|refundPolicyLinkText" link2ur/lib/l10n/app_localizations_en.dart
```

Expected: `3` (or higher — getter declarations + abstract method may double-count).

- [ ] **Step 6: Commit**

```bash
git add link2ur/lib/l10n/app_zh.arb link2ur/lib/l10n/app_en.arb link2ur/lib/l10n/app_zh_Hant.arb link2ur/lib/l10n/app_localizations.dart link2ur/lib/l10n/app_localizations_zh.dart link2ur/lib/l10n/app_localizations_en.dart
git commit -m "feat(l10n): add refund policy strings (zh, en, zh-Hant)"
```

---

## Task 5: Create `RefundPolicyView`

**Files:**
- Create: `link2ur/lib/features/info/views/refund_policy_view.dart`

- [ ] **Step 1: Create the view file**

Create `link2ur/lib/features/info/views/refund_policy_view.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_bloc/flutter_bloc.dart';

import '../../../core/design/app_colors.dart';
import '../../../core/design/app_radius.dart';
import '../../../core/design/app_spacing.dart';
import '../../../core/design/app_typography.dart';
import '../../../core/utils/l10n_extension.dart';
import '../../../data/models/legal_document.dart';
import '../../../data/repositories/common_repository.dart';

/// 退款政策页面
///
/// 通过 GET /api/legal/refund_policy?lang= 获取后端 seeded JSON 文档,
/// 渲染为可滚动的章节卡片列表。
///
/// 入口:
/// - 支付页底部的 RefundPolicyFooter (PaymentView, ApprovalPaymentPage)
/// - 设置页"法律条款"分组的导航行
class RefundPolicyView extends StatelessWidget {
  const RefundPolicyView({super.key});

  @override
  Widget build(BuildContext context) {
    final lang = Localizations.localeOf(context).languageCode;
    // 后端只 seeded zh/en 两种;zh-Hant locale 复用 zh 内容,沿用 FAQView 同款 fallback。
    final apiLang = lang.startsWith('zh') ? 'zh' : 'en';

    final future =
        context.read<CommonRepository>().getLegalDocument(
              type: 'refund_policy',
              lang: apiLang,
            );

    return Scaffold(
      appBar: AppBar(
        title: Text(context.l10n.refundPolicyTitle),
        centerTitle: true,
      ),
      body: FutureBuilder<Map<String, dynamic>>(
        future: future,
        builder: (context, snapshot) {
          if (snapshot.connectionState == ConnectionState.waiting) {
            return const Center(child: CircularProgressIndicator());
          }
          if (snapshot.hasError || snapshot.data == null) {
            return Center(
              child: Padding(
                padding: AppSpacing.allLg,
                child: Text(
                  context.l10n.errorLoadFailedMessage,
                  textAlign: TextAlign.center,
                ),
              ),
            );
          }

          final doc = LegalDocument.fromJson(snapshot.data!);
          final sections = doc.sections;

          return ListView.builder(
            padding: AppSpacing.allMd,
            itemCount: sections.length + 1, // +1 for version footer
            itemBuilder: (context, i) {
              if (i == sections.length) {
                return _VersionFooter(
                  version: doc.version,
                  effectiveAt: doc.effectiveAt,
                );
              }
              return _SectionCard(section: sections[i]);
            },
          );
        },
      ),
    );
  }
}

class _SectionCard extends StatelessWidget {
  const _SectionCard({required this.section});

  final LegalSection section;

  @override
  Widget build(BuildContext context) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    final titleColor =
        isDark ? AppColors.textPrimaryDark : AppColors.textPrimaryLight;
    final bodyColor =
        isDark ? AppColors.textPrimaryDark : AppColors.textPrimaryLight;

    return Container(
      margin: const EdgeInsets.only(bottom: 12),
      padding: AppSpacing.allMd,
      decoration: BoxDecoration(
        color: Theme.of(context).cardColor,
        borderRadius: AppRadius.allMedium,
      ),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          if (section.title.isNotEmpty)
            Padding(
              padding: const EdgeInsets.only(bottom: 6),
              child: Text(
                section.title,
                style: AppTypography.title3.copyWith(color: titleColor),
              ),
            ),
          for (final p in section.paragraphs)
            Padding(
              padding: const EdgeInsets.only(top: 4),
              child: Text(
                p,
                style: AppTypography.body
                    .copyWith(color: bodyColor, height: 1.7),
              ),
            ),
        ],
      ),
    );
  }
}

class _VersionFooter extends StatelessWidget {
  const _VersionFooter({this.version, this.effectiveAt});

  final String? version;
  final String? effectiveAt;

  @override
  Widget build(BuildContext context) {
    final parts = <String>[
      if (version != null && version!.isNotEmpty) 'v$version',
      if (effectiveAt != null && effectiveAt!.isNotEmpty) effectiveAt!,
    ];
    if (parts.isEmpty) return const SizedBox.shrink();
    return Padding(
      padding: const EdgeInsets.symmetric(vertical: 16),
      child: Center(
        child: Text(
          parts.join(' · '),
          style: const TextStyle(
            fontSize: 12,
            color: AppColors.textTertiaryLight,
          ),
        ),
      ),
    );
  }
}
```

**Notes:**
- The view is read-only — no BLoC, no repository event, just a `FutureBuilder`. This matches `FAQView` (`info_views.dart:50`) which is the closest existing precedent.
- `getLegalDocument` already caches results with `staticTTL` inside `CommonRepository` (line 93). No need to add caching here.
- `LegalDocument.title` returns the raw string `title` from `content_json` (which is already shown as the AppBar title via l10n); we deliberately skip rendering it as a section because `_orderKeys` excludes it.
- Section order is guaranteed by Task 3's `_orderKeys('refund_policy')` case.

- [ ] **Step 2: Verify the file compiles**

```powershell
cd F:\python_work\LinkU\link2ur
flutter analyze lib/features/info/views/refund_policy_view.dart
```

Expected: `No issues found!`. If you get import errors, double-check the imports against the existing `info_views.dart` for the right relative paths.

- [ ] **Step 3: Commit**

```bash
git add link2ur/lib/features/info/views/refund_policy_view.dart
git commit -m "feat(info): add RefundPolicyView fetched from /api/legal"
```

---

## Task 6: Add `AppRoutes.refundPolicy` constant + register `GoRoute`

**Files:**
- Modify: `link2ur/lib/core/router/app_routes.dart:158-166`
- Modify: `link2ur/lib/core/router/routes/info_routes.dart:11-58`

- [ ] **Step 1: Add the route constant**

Edit `link2ur/lib/core/router/app_routes.dart`. In the "信息" block (around lines 158-166), add the new constant **after** `communityGuidelines`:

Before:
```dart
  // 信息
  static const String faq = '/faq';
  static const String terms = '/terms';
  static const String privacy = '/privacy';
  static const String communityGuidelines = '/community-guidelines';
  static const String about = '/about';
```

After:
```dart
  // 信息
  static const String faq = '/faq';
  static const String terms = '/terms';
  static const String privacy = '/privacy';
  static const String communityGuidelines = '/community-guidelines';
  static const String refundPolicy = '/info/refund-policy';
  static const String about = '/about';
```

- [ ] **Step 2: Register the GoRoute**

Edit `link2ur/lib/core/router/routes/info_routes.dart`. Add an import and a new `GoRoute` after the `communityGuidelines` route:

Add to imports (after the existing `info_views.dart` import):
```dart
import '../../../features/info/views/refund_policy_view.dart';
```

Insert the new route after the `communityGuidelines` `GoRoute` (after line 36) and before the `about` route (line 37):

```dart
      GoRoute(
        path: AppRoutes.refundPolicy,
        name: 'refundPolicy',
        builder: (context, state) => const RefundPolicyView(),
      ),
```

- [ ] **Step 3: Verify routes compile**

```powershell
cd F:\python_work\LinkU\link2ur
flutter analyze lib/core/router/
```

Expected: `No issues found!`.

- [ ] **Step 4: Commit**

```bash
git add link2ur/lib/core/router/app_routes.dart link2ur/lib/core/router/routes/info_routes.dart
git commit -m "feat(router): wire /info/refund-policy route"
```

---

## Task 7: Create `RefundPolicyFooter` widget

**Files:**
- Create: `link2ur/lib/features/payment/views/widgets/refund_policy_footer.dart`

- [ ] **Step 1: Create the widget file**

Create the directory `link2ur/lib/features/payment/views/widgets/` if it does not exist (the parent `views/` exists; the `widgets/` child likely does not yet — check first with `ls`).

Create `link2ur/lib/features/payment/views/widgets/refund_policy_footer.dart`:

```dart
import 'package:flutter/gestures.dart';
import 'package:flutter/material.dart';
import 'package:go_router/go_router.dart';

import '../../../../core/design/app_colors.dart';
import '../../../../core/router/app_routes.dart';
import '../../../../core/utils/l10n_extension.dart';

/// 支付页底部的退款政策 footer。
///
/// 显示在 PrimaryButton 下方,提示用户支付即视为已阅读并同意《退款政策》。
/// 「《退款政策》」部分可点击,跳转 [AppRoutes.refundPolicy]。
///
/// StatefulWidget 用于 dispose [TapGestureRecognizer]。
class RefundPolicyFooter extends StatefulWidget {
  const RefundPolicyFooter({super.key});

  @override
  State<RefundPolicyFooter> createState() => _RefundPolicyFooterState();
}

class _RefundPolicyFooterState extends State<RefundPolicyFooter> {
  late final TapGestureRecognizer _tapRecognizer;

  @override
  void initState() {
    super.initState();
    _tapRecognizer = TapGestureRecognizer()
      ..onTap = () {
        if (mounted) {
          context.push(AppRoutes.refundPolicy);
        }
      };
  }

  @override
  void dispose() {
    _tapRecognizer.dispose();
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    final baseColor = isDark
        ? AppColors.textSecondaryDark
        : AppColors.textSecondaryLight;

    return Padding(
      padding: const EdgeInsets.only(top: 8, left: 16, right: 16),
      child: Text.rich(
        TextSpan(
          style: TextStyle(fontSize: 12, color: baseColor),
          children: [
            TextSpan(text: context.l10n.refundPolicyFooterPrefix),
            TextSpan(
              text: context.l10n.refundPolicyLinkText,
              style: const TextStyle(
                color: AppColors.primary,
                decoration: TextDecoration.underline,
                fontWeight: FontWeight.w500,
              ),
              recognizer: _tapRecognizer,
            ),
          ],
        ),
        textAlign: TextAlign.center,
      ),
    );
  }
}
```

- [ ] **Step 2: Verify the widget compiles**

```powershell
cd F:\python_work\LinkU\link2ur
flutter analyze lib/features/payment/views/widgets/refund_policy_footer.dart
```

Expected: `No issues found!`.

- [ ] **Step 3: Commit**

```bash
git add link2ur/lib/features/payment/views/widgets/refund_policy_footer.dart
git commit -m "feat(payment): add reusable RefundPolicyFooter widget"
```

---

## Task 8: Widget test for `RefundPolicyFooter`

**Files:**
- Create: `link2ur/test/features/payment/widgets/refund_policy_footer_test.dart`

- [ ] **Step 1: Write the failing test**

Create `link2ur/test/features/payment/widgets/refund_policy_footer_test.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_localizations/flutter_localizations.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:go_router/go_router.dart';

import 'package:link2ur/core/router/app_routes.dart';
import 'package:link2ur/features/payment/views/widgets/refund_policy_footer.dart';
import 'package:link2ur/l10n/app_localizations.dart';

void main() {
  group('RefundPolicyFooter', () {
    testWidgets('renders prefix + link text and navigates on tap',
        (tester) async {
      String? lastPushedLocation;

      final router = GoRouter(
        initialLocation: '/',
        routes: [
          GoRoute(
            path: '/',
            builder: (_, __) => const Scaffold(body: RefundPolicyFooter()),
          ),
          GoRoute(
            path: AppRoutes.refundPolicy,
            builder: (_, __) {
              lastPushedLocation = AppRoutes.refundPolicy;
              return const Scaffold(body: Text('REFUND POLICY VIEW STUB'));
            },
          ),
        ],
      );

      await tester.pumpWidget(
        MaterialApp.router(
          routerConfig: router,
          localizationsDelegates: AppLocalizations.localizationsDelegates,
          supportedLocales: AppLocalizations.supportedLocales,
          locale: const Locale('en'),
        ),
      );
      await tester.pumpAndSettle();

      // Both spans should be present
      expect(find.textContaining('By tapping Pay'), findsOneWidget);
      expect(find.textContaining('Refund Policy'), findsOneWidget);

      // Tap on the linked text — RichText hit testing requires tapping the
      // RichText finder and using a TestGesture pointed at the link span.
      final richText = tester.widget<RichText>(find.byType(RichText).first);
      // Walk the inline spans to find the linked one (the underlined,
      // primary-colored span with a TapGestureRecognizer).
      InlineSpan? linkSpan;
      richText.text.visitChildren((span) {
        if (span is TextSpan && span.recognizer != null) {
          linkSpan = span;
          return false; // stop visiting
        }
        return true;
      });
      expect(linkSpan, isNotNull, reason: 'Link span with tap recognizer must exist');

      // Trigger the tap directly on the recognizer (simulates a hit-test).
      (linkSpan! as TextSpan).recognizer!
          .addPointer(PointerDownEvent(position: Offset.zero));
      // The recognizer's onTap fires immediately on a synthesized tap when we
      // call onTap directly via the inspected gesture — simpler approach:
      ((linkSpan! as TextSpan).recognizer as TapGestureRecognizer).onTap?.call();
      await tester.pumpAndSettle();

      expect(lastPushedLocation, AppRoutes.refundPolicy);
    });

    testWidgets('disposes cleanly without leaking the gesture recognizer',
        (tester) async {
      // If dispose() is missing the test framework will report a leak.
      await tester.pumpWidget(
        MaterialApp(
          localizationsDelegates: AppLocalizations.localizationsDelegates,
          supportedLocales: AppLocalizations.supportedLocales,
          locale: const Locale('en'),
          home: const Scaffold(body: RefundPolicyFooter()),
        ),
      );
      await tester.pumpAndSettle();
      // Pump a different widget to dispose RefundPolicyFooter
      await tester.pumpWidget(const SizedBox.shrink());
      // Test passes if no leak warnings are raised by Flutter's framework.
    });
  });
}
```

- [ ] **Step 2: Run the test**

```powershell
cd F:\python_work\LinkU\link2ur
flutter test test/features/payment/widgets/refund_policy_footer_test.dart
```

Expected: BOTH tests PASS. If the link-tap test fails because `recognizer.onTap` is null on the inspected span, the issue is most likely that the test grabbed the wrong RichText (the AppBar title can also be a RichText). In that case, replace `find.byType(RichText).first` with a more specific finder — e.g. `find.descendant(of: find.byType(RefundPolicyFooter), matching: find.byType(RichText))`.

- [ ] **Step 3: Commit**

```bash
git add link2ur/test/features/payment/widgets/refund_policy_footer_test.dart
git commit -m "test(payment): RefundPolicyFooter renders + navigates on tap"
```

---

## Task 9: Wire `RefundPolicyFooter` into `PaymentView`

**Files:**
- Modify: `link2ur/lib/features/payment/views/payment_view.dart` (around line 1142, in `_buildPayButton`'s `SafeArea`)

- [ ] **Step 1: Add the import**

In `link2ur/lib/features/payment/views/payment_view.dart`, add this import alongside the other relative imports near the top of the file:

```dart
import 'widgets/refund_policy_footer.dart';
```

- [ ] **Step 2: Embed the footer below the PrimaryButton**

Locate the `_buildPayButton` method (around line 1099). Inside the `SafeArea` (around line 1142-1150), the current code is:

```dart
      child: SafeArea(
        child: PrimaryButton(
          text: buttonText,
          isLoading: state.isProcessing,
          onPressed: (state.isProcessing || _alreadyPaid) ? null : _processPayment,
          icon: isWalletOnly ? Icons.account_balance_wallet : _payButtonIconData(),
          leading: isWalletOnly ? null : _payButtonLeadingWidget(),
        ),
      ),
```

Wrap the `PrimaryButton` in a `Column` and add the footer below:

```dart
      child: SafeArea(
        child: Column(
          mainAxisSize: MainAxisSize.min,
          children: [
            PrimaryButton(
              text: buttonText,
              isLoading: state.isProcessing,
              onPressed: (state.isProcessing || _alreadyPaid) ? null : _processPayment,
              icon: isWalletOnly ? Icons.account_balance_wallet : _payButtonIconData(),
              leading: isWalletOnly ? null : _payButtonLeadingWidget(),
            ),
            const RefundPolicyFooter(),
          ],
        ),
      ),
```

- [ ] **Step 3: Verify it compiles**

```powershell
cd F:\python_work\LinkU\link2ur
flutter analyze lib/features/payment/views/payment_view.dart
```

Expected: `No issues found!`. If `payment_widgets.dart` is `part of 'payment_view.dart'` and somehow shadows the import, just put the import above the `part` declaration.

- [ ] **Step 4: Commit**

```bash
git add link2ur/lib/features/payment/views/payment_view.dart
git commit -m "feat(payment): show RefundPolicyFooter under Pay button on PaymentView"
```

---

## Task 10: Wire `RefundPolicyFooter` into `ApprovalPaymentPage`

**Files:**
- Modify: `link2ur/lib/features/tasks/views/approval_payment_page.dart:978`

- [ ] **Step 1: Add the import**

In `link2ur/lib/features/tasks/views/approval_payment_page.dart`, add this import alongside the existing relative imports near the top of the file:

```dart
import '../../payment/views/widgets/refund_policy_footer.dart';
```

- [ ] **Step 2: Embed the footer below the PrimaryButton**

Locate the `PrimaryButton` at approximately line 978 (inside `build()`):

```dart
                      PrimaryButton(
                        text: _payButtonText(l10n),
                        icon: _payButtonIcon(),
                        leading: _payButtonLeading(),
                        isLoading: _isProcessing,
                        onPressed: (_isProcessing || _alreadyPaid) ? null : _pay,
                      ),
                    ],
                  ),
                ),
```

Add a `const RefundPolicyFooter()` immediately AFTER the `PrimaryButton`, inside the same `Column` that already contains it:

```dart
                      PrimaryButton(
                        text: _payButtonText(l10n),
                        icon: _payButtonIcon(),
                        leading: _payButtonLeading(),
                        isLoading: _isProcessing,
                        onPressed: (_isProcessing || _alreadyPaid) ? null : _pay,
                      ),
                      const RefundPolicyFooter(),
                    ],
                  ),
                ),
```

(The exact indentation must match the surrounding `PrimaryButton` line — copy-paste from there, do not retype.)

- [ ] **Step 3: Verify it compiles**

```powershell
cd F:\python_work\LinkU\link2ur
flutter analyze lib/features/tasks/views/approval_payment_page.dart
```

Expected: `No issues found!`.

- [ ] **Step 4: Commit**

```bash
git add link2ur/lib/features/tasks/views/approval_payment_page.dart
git commit -m "feat(payment): show RefundPolicyFooter on ApprovalPaymentPage"
```

---

## Task 11: Add the settings "退款政策" nav row

**Files:**
- Modify: `link2ur/lib/features/settings/views/settings_view.dart:304-344`

- [ ] **Step 1: Add the import**

In `link2ur/lib/features/settings/views/settings_view.dart`, add these imports near the existing relative imports:

```dart
import 'package:go_router/go_router.dart';

import '../../../core/router/app_routes.dart';
```

(If `go_router` is already imported, skip the first line. Same for `app_routes.dart`.)

- [ ] **Step 2: Add the nav row**

Inside the "法律条款" `_SettingsSection` (around lines 304-344), insert a NEW `_SettingsNavRow` **after** the Cookie Policy row and **before** the Community Guidelines row. The existing block:

```dart
                        _settingsDivider(isDark),
                        _SettingsNavRow(
                          icon: Icons.cookie_outlined,
                          title: context.l10n.settingsCookiePolicy,
                          onTap: () => _openInAppWebView(
                            'https://link2ur.com/cookies',
                            context.l10n.settingsCookiePolicy,
                          ),
                        ),
                        _settingsDivider(isDark),
                        _SettingsNavRow(
                          icon: Icons.groups_outlined,
                          title: context.l10n.settingsCommunityGuidelines,
                          onTap: () => _openInAppWebView(
                            'https://link2ur.com/community-guidelines',
                            context.l10n.settingsCommunityGuidelines,
                          ),
                        ),
```

becomes:

```dart
                        _settingsDivider(isDark),
                        _SettingsNavRow(
                          icon: Icons.cookie_outlined,
                          title: context.l10n.settingsCookiePolicy,
                          onTap: () => _openInAppWebView(
                            'https://link2ur.com/cookies',
                            context.l10n.settingsCookiePolicy,
                          ),
                        ),
                        _settingsDivider(isDark),
                        _SettingsNavRow(
                          icon: Icons.account_balance_outlined,
                          title: context.l10n.refundPolicyTitle,
                          onTap: () => context.push(AppRoutes.refundPolicy),
                        ),
                        _settingsDivider(isDark),
                        _SettingsNavRow(
                          icon: Icons.groups_outlined,
                          title: context.l10n.settingsCommunityGuidelines,
                          onTap: () => _openInAppWebView(
                            'https://link2ur.com/community-guidelines',
                            context.l10n.settingsCommunityGuidelines,
                          ),
                        ),
```

**Note:** unlike the sibling rows that open `_openInAppWebView`, the refund policy row uses `context.push(AppRoutes.refundPolicy)` because we have a true in-app page. (Sibling legal docs still use the WebView approach against `link2ur.com`; migrating them is out of scope for this plan.)

- [ ] **Step 3: Verify it compiles**

```powershell
cd F:\python_work\LinkU\link2ur
flutter analyze lib/features/settings/views/settings_view.dart
```

Expected: `No issues found!`.

- [ ] **Step 4: Commit**

```bash
git add link2ur/lib/features/settings/views/settings_view.dart
git commit -m "feat(settings): add refund policy entry under 法律条款 section"
```

---

## Task 12: Full-stack consistency check + manual smoke test

This task does NOT change code. It is the verification gate per `CLAUDE.md` "Full-Stack Consistency Check" rule, before pushing main.

- [ ] **Step 1: Run the project's full analyzer + tests**

```powershell
cd F:\python_work\LinkU\link2ur
$env:PATH = "F:\flutter\bin;" + $env:PATH; $env:PUB_CACHE = "F:\DevCache\.pub-cache"
flutter analyze
flutter test
```

Expected: zero analyzer issues; all tests pass (existing tests + the 3 new ones from Tasks 3 + 8).

- [ ] **Step 2: Push to main**

```bash
git push origin main
```

This triggers Railway autodeploy of the backend (no-op, since Task 1's migration was already applied directly via psql in Task 2) and triggers no app store update — Flutter changes are validated locally only.

- [ ] **Step 3: Wait for linktest deploy to settle, then manual smoke**

Wait for the `linktest.up.railway.app` deploy to complete (check Railway dashboard or `curl https://linktest.up.railway.app/health`).

Then run the app locally pointed at linktest:

```powershell
cd F:\python_work\LinkU\link2ur
$env:PATH = "F:\flutter\bin;" + $env:PATH; $env:PUB_CACHE = "F:\DevCache\.pub-cache"
flutter run -d web-server
```

Open the printed URL in a browser. With a logged-in test account:

- [ ] **Step 4: Verify entry point — task payment**

Post a paid task or open an existing task that requires payment → land on `PaymentView` → confirm the small grey footer text "支付即视为已阅读并同意《退款政策》" appears below the Pay button → tap "《退款政策》" → confirm a new page titled "退款政策" opens with all 13 content sections rendered + version footer "v1.0 · 2026-05-03" at the bottom.

- [ ] **Step 5: Verify entry point — approval payment**

Open a task / activity / flea-market rental that triggers `ApprovalPaymentPage` → confirm the same footer appears → tap → same page opens.

- [ ] **Step 6: Verify entry point — settings**

Open Profile tab → Settings → "法律条款" section → tap "退款政策" → same page opens.

- [ ] **Step 7: Verify language fallback**

Switch app language in Settings to English → return to refund policy page → page reloads with English content. Switch to 繁體中文 → page falls back to zh content (intentional — see spec §6.3).

- [ ] **Step 8: Verify the API endpoint on prod (after promotion to prod)**

Once linktest is verified and you decide to promote to production:

```powershell
psql $env:PROD_DB -f F:\python_work\LinkU\backend\migrations\223_seed_refund_policy_legal_document.sql
curl -s "https://api.link2ur.com/api/legal/refund_policy?lang=zh" | jq '.version'
```

Expected: `"v1.0"`.

- [ ] **Step 9: (Optional) Update auto-memory if anything was learned**

If during smoke testing you discovered something non-obvious about the legal-doc API or LegalDocument model that future-you would want to recall, save it as an architecture memory under `C:\Users\Ryan\.claude\projects\F--python-work-LinkU\memory\`. Otherwise skip.

---

## Self-Review Checklist (run after writing the plan)

**Spec coverage** (cross-checking against `docs/superpowers/specs/2026-05-03-refund-policy-page-design.md`):

| Spec section | Implemented in task |
|---|---|
| §5.1 Migration 223 (zh + en) | Task 1 |
| §5.3 Migration runbook (linktest before prod) | Task 2, Task 12 step 8 |
| §6.1 New file `refund_policy_view.dart` | Task 5 |
| §6.1 New file `refund_policy_footer.dart` | Task 7 |
| §6.2 Edit `legal_document.dart` `_orderKeys` | Task 3 |
| §6.2 Edit `app_routes.dart` constant | Task 6 step 1 |
| §6.2 Edit `info_routes.dart` GoRoute | Task 6 step 2 |
| §6.2 Edit `payment_view.dart` | Task 9 |
| §6.2 Edit `approval_payment_page.dart` | Task 10 |
| §6.2 Edit `settings_view.dart` legal section | Task 11 |
| §6.2 Edit 3 ARB files + gen-l10n | Task 4 |
| §6.3 RefundPolicyView FutureBuilder + ListView | Task 5 |
| §6.4 RefundPolicyFooter `StatefulWidget` + dispose recognizer | Task 7 |
| §6.5 Three l10n keys | Task 4 |
| §7 Refund policy zh content (14 sections) | Task 1 (zh INSERT) |
| §7 Refund policy en content (1:1 translation) | Task 1 (en INSERT) |
| §8 4 integration points | **Reduced to 3**: PaymentView (T9) + ApprovalPaymentPage (T10) + Settings (T11). **Wallet top-up dropped** — the wallet view has no top-up flow; documented in plan Notes. |
| §9 Unit test for `_orderKeys` | Task 3 |
| §9 Widget test for footer | Task 8 |
| §9 Manual smoke (4 entry points + 3 langs) | Task 12 — adjusted to 3 entry points |
| §10 Rollout (linktest → prod) | Task 2 + Task 12 step 8 |

**Discrepancy:** Spec §8 lists wallet top-up as a 4th surface; this plan drops it (no top-up flow exists). Discovery noted in plan Notes.

**Placeholder scan:** No "TBD"/"TODO"/"figure out later" remain. The one "may not exist — check first with `ls`" instruction in Task 7 step 1 is a concrete branching instruction, not a placeholder.

**Type consistency:** `RefundPolicyView`, `RefundPolicyFooter`, `AppRoutes.refundPolicy`, `refundPolicyTitle`/`refundPolicyFooterPrefix`/`refundPolicyLinkText`, `_orderKeys('refund_policy')`, migration filename `223_seed_refund_policy_legal_document.sql`, doc type `'refund_policy'` — all consistent across all task references.
