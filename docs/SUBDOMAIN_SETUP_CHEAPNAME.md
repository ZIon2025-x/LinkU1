# 在 CheapName 上配置 admin 子域名

本指南说明如何在 CheapName 域名注册商配置 `admin.link2ur.com` 子域名。

## 前置条件

- 你的域名 `link2ur.com` 在 CheapName 注册
- 你已经准备好部署 admin 应用的服务器或平台（如 Vercel、Railway 等）

## 方法 1: 使用 CNAME 记录（推荐，适用于 Vercel 等平台）

**你的项目已配置 Vercel 部署**，使用 CNAME 记录是最简单的方法。

### 完整步骤（Vercel + CheapName）

#### 第一步：在 Vercel 添加域名

1. **登录 Vercel**
   - 访问 https://vercel.com
   - 登录你的账户
   - 找到你的 admin 项目（`linku-admin`）

2. **添加自定义域名**
   - 进入项目 → **Settings** → **Domains**
   - 点击 **Add Domain**
   - 输入：`admin.link2ur.com`
   - 点击 **Add**

3. **获取 CNAME 记录值**
   - Vercel 会显示 DNS 配置信息
   - 你会看到类似这样的信息：
     ```
     类型: CNAME
     名称: admin
     值: cname.vercel-dns.com  (这个值每个人可能不同)
     ```
   - **复制这个 CNAME 值**（例如：`cname.vercel-dns.com` 或 `76.76.21.21`）

#### 第二步：在 CheapName 配置 DNS

1. **登录 CheapName 账户**
   - 访问 https://www.cheapname.com
   - 登录你的账户

2. **进入 DNS 管理**
   - 在域名列表中找到 `link2ur.com`
   - 点击域名或"管理"按钮
   - 找到 **"DNS 管理"** 或 **"DNS 设置"** 或 **"域名解析"**
   
   > 💡 **提示**：如果找不到 DNS 设置，可能叫：
   > - "DNS 记录"
   > - "域名解析"
   > - "DNS 配置"
   > - "Name Servers"

3. **添加 CNAME 记录**
   - 点击 **"添加记录"** 或 **"添加 DNS 记录"** 或 **"+"** 按钮
   - 填写以下信息：
     ```
     记录类型: CNAME
     主机名/名称/子域名: admin
     值/目标/指向: [粘贴从 Vercel 复制的 CNAME 值]
     TTL: 3600 (或选择"自动")
     ```
   
   **示例：**
   ```
   类型: CNAME
   名称: admin
   值: cname.vercel-dns.com
   TTL: 3600
   ```

4. **保存记录**
   - 点击 **"保存"** 或 **"添加"** 或 **"确认"**
   - 记录添加成功

#### 第三步：等待 DNS 传播

- **通常需要 5-30 分钟**
- **最多 48 小时**（全球传播）
- 可以在 Vercel 的 Domains 页面看到状态：
  - 🟡 **Pending** = 等待 DNS 验证
  - 🟢 **Valid** = DNS 配置成功

#### 第四步：验证配置

1. **在 Vercel 检查**
   - 回到 Vercel → Settings → Domains
   - 如果看到 ✅ **Valid**，说明配置成功

2. **使用命令行验证**
   ```bash
   # 检查 CNAME 记录
   dig admin.link2ur.com CNAME
   
   # 或使用 nslookup
   nslookup admin.link2ur.com
   ```

3. **浏览器测试**
   - 等待 DNS 传播后（5-30 分钟）
   - 访问：`https://admin.link2ur.com`
   - 应该能看到你的管理后台登录页面

### Vercel 自动 SSL 证书

✅ **好消息**：Vercel 会自动为你的域名配置 SSL 证书（HTTPS）
- 无需手动配置
- 完全免费
- 自动续期
- 通常在 DNS 验证成功后几分钟内自动配置

## 方法 2: 使用 A 记录（适用于独立服务器）

如果你的 admin 应用部署在独立服务器（如 VPS、云服务器），使用 A 记录。

### 步骤

1. **获取服务器 IP 地址**
   ```bash
   # 如果你使用 Railway、Render 等平台，查看服务详情获取 IP
   # 或者使用 nslookup 查询
   nslookup your-server-domain.com
   ```

2. **在 CheapName 添加 A 记录**
   - 进入 DNS 管理
   - 添加新记录：
     ```
     主机名/名称: admin
     类型: A
     值/目标: 你的服务器IP地址 (例如: 203.0.113.1)
     TTL: 3600
     ```

## 方法 3: 使用 Cloudflare（推荐，免费且功能强大）

如果你使用 Cloudflare 作为 DNS 提供商，配置会更简单。

### 步骤

1. **将域名添加到 Cloudflare**
   - 注册 Cloudflare 账户（免费）
   - 添加你的域名 `link2ur.com`
   - 按照提示更改 CheapName 的 Nameserver 为 Cloudflare 提供的

2. **在 Cloudflare 添加 CNAME 记录**
   - 进入 DNS 设置
   - 添加记录：
     ```
     类型: CNAME
     名称: admin
     目标: 你的部署平台提供的地址
     代理状态: 已代理（橙色云）或仅 DNS（灰色云）
     ```

3. **更新 CheapName Nameserver**
   - 在 CheapName 找到"Nameserver 设置"
   - 更改为 Cloudflare 提供的 Nameserver（通常是 `xxx.ns.cloudflare.com`）

## 具体平台配置

### Vercel 部署

1. **在 Vercel 添加域名**
   ```bash
   # 在 Vercel 项目设置中
   Settings → Domains → Add Domain
   输入: admin.link2ur.com
   ```

2. **获取 CNAME 目标**
   - Vercel 会显示类似 `cname.vercel-dns.com` 的地址
   - 复制这个地址

3. **在 CheapName 配置**
   - 添加 CNAME 记录：`admin` → `cname.vercel-dns.com`

### Railway 部署

1. **在 Railway 配置自定义域名**
   - 项目设置 → Settings → Domains
   - 添加 `admin.link2ur.com`

2. **Railway 会提供 DNS 配置**
   - 通常是 CNAME 记录
   - 按照 Railway 提供的值在 CheapName 配置

### 独立服务器（VPS/云服务器）

1. **配置 Web 服务器（Nginx/Apache）**
   ```nginx
   # Nginx 配置示例
   server {
       listen 80;
       server_name admin.link2ur.com;
       
       location / {
           proxy_pass http://localhost:3001;
           proxy_set_header Host $host;
           proxy_set_header X-Real-IP $remote_addr;
       }
   }
   ```

2. **在 CheapName 添加 A 记录**
   - `admin` → 你的服务器 IP

## 验证 DNS 配置

配置完成后，验证 DNS 是否生效：

### 方法 1: 使用命令行
```bash
# 检查 CNAME 记录
dig admin.link2ur.com CNAME

# 检查 A 记录
dig admin.link2ur.com A

# 或使用 nslookup
nslookup admin.link2ur.com
```

### 方法 2: 使用在线工具
- https://dnschecker.org
- https://www.whatsmydns.net
- https://mxtoolbox.com/DNSLookup.aspx

输入 `admin.link2ur.com`，查看是否解析到正确的地址。

### 方法 3: 浏览器测试
```bash
# 等待 DNS 传播后（通常 5-30 分钟）
# 在浏览器访问
https://admin.link2ur.com
```

## SSL 证书配置

### 自动 SSL（推荐）

- **Vercel/Netlify**: 自动提供免费 SSL 证书
- **Cloudflare**: 自动提供 SSL（需要启用代理）
- **Let's Encrypt**: 在服务器上使用 Certbot 自动获取

### 手动配置 SSL

如果使用独立服务器：

```bash
# 使用 Certbot 获取 Let's Encrypt 证书
sudo certbot --nginx -d admin.link2ur.com
```

## 常见问题

### 1. DNS 传播需要多长时间？
- 通常 5-30 分钟
- 最多 48 小时（全球传播）
- 使用 `dig` 或在线工具检查传播状态

### 2. 如何知道 DNS 是否生效？
```bash
# 检查解析结果
dig admin.link2ur.com +short
# 应该返回你的服务器 IP 或 CNAME 目标
```

### 3. 子域名可以指向不同的服务器吗？
可以。每个子域名可以独立配置，指向不同的服务器或服务。

### 4. 需要为子域名单独购买 SSL 证书吗？
不需要。Let's Encrypt 等免费证书支持子域名，Vercel/Cloudflare 等平台自动提供。

### 5. CheapName 界面找不到 DNS 设置？
- 查找"域名管理"、"DNS 管理"、"域名设置"
- 或联系 CheapName 客服获取帮助
- 某些域名可能需要先激活 DNS 管理功能

## 配置示例

### CheapName DNS 记录示例

假设你的主域名配置：
```
类型    主机名    值                    TTL
A       @         203.0.113.1          3600
CNAME   www       link2ur.com          3600
```

添加 admin 子域名后：
```
类型    主机名    值                    TTL
A       @         203.0.113.1          3600
CNAME   www       link2ur.com          3600
CNAME   admin     cname.vercel-dns.com  3600  ← 新增
```

## 下一步

DNS 配置完成后：

1. ✅ 验证 DNS 解析
2. ✅ 配置 SSL 证书（如果未自动配置）
3. ✅ 测试 `https://admin.link2ur.com` 访问
4. ✅ 更新后端 CORS 配置（如果还未完成）
5. ✅ 部署 admin 应用到目标平台

## 需要帮助？

如果遇到问题：
1. 检查 CheapName 的 DNS 管理界面是否有特殊要求
2. 联系 CheapName 客服获取 DNS 配置帮助
3. 查看你的部署平台（Vercel/Railway）的域名配置文档
