# AdminDashboard 新架构说明

## 📐 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                      User Browser                            │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                   App.tsx (路由入口)                          │
│                         │                                    │
│                         ▼                                    │
│              /admin/* → AdminRoutes                          │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                    AdminLayout                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Sidebar  │            Main Content Area              │  │
│  │           │  ┌────────────────────────────────────┐   │  │
│  │  - 导航   │  │        Top Bar                     │   │  │
│  │  - 菜单   │  └────────────────────────────────────┘   │  │
│  │           │  ┌────────────────────────────────────┐   │  │
│  │           │  │                                    │   │  │
│  │           │  │     <Lazy Loaded Module>          │   │  │
│  │           │  │                                    │   │  │
│  │           │  │  使用 Suspense 懒加载               │   │  │
│  │           │  │                                    │   │  │
│  │           │  └────────────────────────────────────┘   │  │
│  │           │  ┌────────────────────────────────────┐   │  │
│  │           │  │        Footer                      │   │  │
│  │           │  └────────────────────────────────────┘   │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## 🏛️ 分层架构

```
┌─────────────────────────────────────────────────────────┐
│                    表现层 (View)                         │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐    │
│  │   Coupons    │ │    Users     │ │   Experts    │    │
│  │   Module     │ │   Module     │ │   Module     │    │
│  └──────────────┘ └──────────────┘ └──────────────┘    │
│           │                │                │           │
└───────────┼────────────────┼────────────────┼───────────┘
            │                │                │
            ▼                ▼                ▼
┌─────────────────────────────────────────────────────────┐
│                   共享层 (Shared)                        │
│  ┌─────────────────────────────────────────────────┐    │
│  │  Hooks                                          │    │
│  │  - useAdminTable (表格逻辑)                      │    │
│  │  - useModalForm (表单逻辑)                       │    │
│  │  - useAdminApi (API 逻辑)                        │    │
│  └─────────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────────┐    │
│  │  Components                                     │    │
│  │  - AdminTable (表格组件)                         │    │
│  │  - AdminModal (模态框)                           │    │
│  │  - AdminPagination (分页)                        │    │
│  │  - StatusBadge (状态标签)                        │    │
│  └─────────────────────────────────────────────────┘    │
└───────────────────────────┬─────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────┐
│                   数据层 (Data)                          │
│  ┌─────────────────────────────────────────────────┐    │
│  │  API Functions                                  │    │
│  │  - getCoupons(), createCoupon()                 │    │
│  │  - getUsers(), updateUser()                     │    │
│  │  - ...                                          │    │
│  └─────────────────────────────────────────────────┘    │
└───────────────────────────┬─────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────┐
│                 Backend API                              │
│              FastAPI / Python                            │
└─────────────────────────────────────────────────────────┘
```

## 📦 模块结构

### 单个模块的标准结构

```
pages/admin/my-module/
│
├── MyModule.tsx                  # 主组件 (使用 hooks)
│   ├── useAdminTable            # 数据管理
│   ├── useModalForm             # 表单管理
│   └── 业务逻辑                  # 特定逻辑
│
├── MyModuleForm.tsx              # 表单组件（可选）
│   └── 复杂表单逻辑
│
├── types.ts                      # TypeScript 类型
│   ├── interface DataType
│   ├── interface FormData
│   └── const initialValues
│
├── MyModule.module.css           # 样式
├── MyModuleForm.module.css       # 表单样式（可选）
│
├── utils.ts                      # 工具函数（可选）
└── index.ts                      # 导出
```

## 🔄 数据流

### 1. 列表加载流程

```
用户访问页面
    │
    ▼
模块组件挂载
    │
    ▼
useAdminTable Hook
    │
    ├── 初始化状态 (page=1, loading=false)
    │
    ├── useEffect 触发
    │   │
    │   ├── setLoading(true)
    │   │
    │   ├── fetchData({ page, pageSize, filters })
    │   │   │
    │   │   └── API 调用
    │   │       │
    │   │       ├── Success → setData(), setTotal()
    │   │       │
    │   │       └── Error → onError(), setData([])
    │   │
    │   └── setLoading(false)
    │
    └── 返回 { data, loading, currentPage, ... }
        │
        ▼
组件渲染
    │
    ├── <AdminTable data={table.data} />
    │
    └── <AdminPagination {...table} />
```

### 2. 创建/编辑流程

```
用户点击「创建」按钮
    │
    ▼
modal.open()                      # 或 modal.open(editData)
    │
    ├── setIsOpen(true)
    ├── setIsEdit(false/true)
    └── setFormData(initialValues/editData)
    │
    ▼
<AdminModal isOpen={true}>
    │
    ├── 用户编辑表单
    │   │
    │   └── modal.updateField(field, value)
    │
    └── 用户点击提交
        │
        ▼
modal.handleSubmit()
    │
    ├── setLoading(true)
    │
    ├── onSubmit(formData, isEdit)
    │   │
    │   ├── API 调用 (create/update)
    │   │   │
    │   │   ├── Success
    │   │   │   ├── onSuccess()
    │   │   │   ├── modal.close()
    │   │   │   └── table.refresh()
    │   │   │
    │   │   └── Error → onError()
    │   │
    │   └── setLoading(false)
    │
    └── 表单重置
```

### 3. 删除流程

```
用户点击「删除」按钮
    │
    ▼
handleDelete(id)
    │
    ├── 确认对话框
    │   │
    │   ├── 取消 → 结束
    │   │
    │   └── 确定
    │       │
    │       ▼
    │   deleteApi(id)
    │       │
    │       ├── Success
    │       │   ├── message.success()
    │       │   └── table.refresh()
    │       │
    │       └── Error → message.error()
    │
    └── 结束
```

## 🎨 组件关系图

```
AdminLayout
    │
    ├── Sidebar
    │   └── Navigation Menu
    │
    ├── TopBar
    │   ├── Breadcrumb
    │   └── UserMenu
    │
    ├── Content Area
    │   │
    │   └── <Module Component>
    │       │
    │       ├── useAdminTable
    │       │   └── AdminTable
    │       │       └── AdminPagination
    │       │
    │       ├── useModalForm
    │       │   └── AdminModal
    │       │       └── Form Fields
    │       │
    │       └── useAdminApi
    │           └── Action Buttons
    │
    └── Footer
```

## 🔌 Hook 依赖关系

```
useAdminTable
    │
    ├── useState (内部状态)
    │   ├── data, loading
    │   ├── currentPage, pageSize
    │   ├── searchTerm, filters
    │   └── sortField, sortOrder
    │
    ├── useEffect (数据加载)
    │   └── fetchData()
    │
    └── useCallback (优化性能)
        ├── setCurrentPage
        ├── setSearchTerm
        ├── setFilters
        └── refresh

useModalForm
    │
    ├── useState (内部状态)
    │   ├── isOpen, isEdit
    │   ├── formData
    │   └── loading
    │
    └── useCallback (优化性能)
        ├── open
        ├── close
        ├── updateField
        └── handleSubmit

useAdminApi
    │
    ├── useState (内部状态)
    │   ├── data, loading
    │   └── error
    │
    └── useCallback
        ├── execute
        └── reset
```

## 📈 性能优化策略

```
1. 代码分割 (Code Splitting)
   ├── React.lazy() - 组件懒加载
   └── Webpack - 自动 chunk 分割

2. 路由懒加载
   └── Suspense + lazy import

3. 状态隔离
   ├── 每个模块独立状态
   └── 避免全局状态污染

4. Memo 优化
   ├── useCallback - 函数缓存
   ├── useMemo - 计算缓存
   └── React.memo - 组件缓存

5. 虚拟滚动（可选）
   └── 大列表性能优化
```

## 🛠️ 扩展点

### 添加新的共享 Hook

```typescript
// hooks/useMyHook.ts
export function useMyHook(config) {
  // 实现
}

// hooks/index.ts
export * from './useMyHook';
```

### 添加新的共享组件

```typescript
// components/admin/MyComponent.tsx
export const MyComponent = () => { ... }

// components/admin/index.ts
export * from './MyComponent';
```

### 添加新模块

1. 创建目录结构
2. 实现组件（使用共享 hooks/components）
3. 添加路由
4. 添加菜单项

## 📊 对比：重构前后

### 重构前
```
AdminDashboard.tsx (12,571 行)
    ├── 177 个状态变量
    ├── 所有模块混在一起
    ├── 难以维护
    ├── 无法代码分割
    └── 性能问题
```

### 重构后
```
共享层 (Hooks + Components)
    ├── 高复用性
    └── 易于维护

模块层 (独立模块)
    ├── 优惠券模块 (~280 行)
    ├── 用户模块 (~200 行)
    ├── 专家模块 (~250 行)
    └── ... (其他模块)

特点:
    ├── 职责单一
    ├── 易于测试
    ├── 代码分割
    └── 性能优化
```

---

**版本**: 1.0
**创建日期**: 2025-02-05
**更新日期**: 2025-02-05
